// WeftIPC stub — socket server lands with M2 (needed for keybind dispatch).
// `weftctl subscribe` (event bus) lands with the M2 reducer debug trace.
// Exists in M1 only so the package layout is final.
public enum WeftIPCStub {
    public static let milestone = "M2"
}
